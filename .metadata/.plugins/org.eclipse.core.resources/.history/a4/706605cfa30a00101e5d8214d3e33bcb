/*
 * generated by Xtext 2.37.0
 */
package org.xtext.example.tfl.scoping;

import org.eclipse.emf.ecore.EReference
import org.eclipse.xtext.scoping.IScope
import org.eclipse.xtext.scoping.Scopes
import org.eclipse.xtext.scoping.impl.AbstractDeclarativeScopeProvider
import org.xtext.example.tfl.tfl.Disruption
import org.xtext.example.tfl.tfl.Route

/**
 * This class contains custom scoping description.
 * 
 * See https://www.eclipse.org/Xtext/documentation/303_runtime_concepts.html#scoping
 * on how and when to use it.
 */
public class TflScopeProvider extends AbstractDeclarativeScopeProvider {
	    // Provide scope for Route start and end (must refer to a Stop)
    def IScope scope_Route_start(Route route, EReference ref) {
        return Scopes.scopeFor(route.eContainer.eContainer.stops)
    }

    def IScope scope_Route_end(Route route, EReference ref) {
        return Scopes.scopeFor(route.eContainer.eContainer.stops)
    }

    // Provide scope for Disruption transport (must refer to a Transport)
    def IScope scope_Disruption_transport(Disruption disruption, EReference ref) {
        return Scopes.scopeFor(disruption.eContainer.transports)
    }

    // Provide scope for Disruption stop (must refer to a Stop)
    def IScope scope_Disruption_stop(Disruption disruption, EReference ref) {
        return Scopes.scopeFor(disruption.eContainer.stops)
    }
	
}
